name: proyecto_food_log
description: "Proyecto de comida en restaurantes"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.5.0+2

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  sqlite3_flutter_libs: ^0.5.26
  path: ^1.9.1
  uuid: ^4.5.3
  flutter_riverpod: ^3.3.2
  drift: 2.31.0
  file_picker: ^11.0.2
  shared_preferences: ^2.5.5
  image_picker: ^1.2.3
  archive: ^4.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  path_provider_platform_interface: ^2.1.2
  sqlite3: ^2.9.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: 2.31.0
  build_runner: ^2.15.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# Config for the flutter_launcher_icons generator (dart run flutter_launcher_icons).
# Uses assets/branding/icon.png (icon only, no wordmark) as the source for every platform.
flutter_launcher_icons:
  image_path: "assets/branding/icon.png"
  android: true
  adaptive_icon_background: "#EFE0C9"
  adaptive_icon_foreground: "assets/branding/icon.png"
  ios: true
  remove_alpha_ios: true
  macos:
    generate: true
  windows:
    generate: true
  web:
    generate: true

# Config for the flutter_native_splash generator (dart run flutter_native_splash:create).
# Shows the icon centered with the "Food Log" wordmark pinned below it as a
# branding image. On Android 12+ this relies on the native
# windowSplashScreenBrandingImage API (android_12 section below) instead of a
# second Flutter-side screen — without it, Android 12+ ignores this config
# entirely and falls back to a plain icon on a black background.
#
# icon.png's line art is brandNavy, which disappears against the dark
# background — icon_dark.png is the same icon recolored to creamBackground
# so it stays visible in dark mode. wordmark.png/wordmark_dark.png are the
# "FOOD LOG" crop recolored to brandOrangeInk (light, AA-safe) and flat
# brandOrange (dark, already high-contrast on the sepia background) to match
# lib/core/theme/app_theme.dart's own light/dark ink logic.
flutter_native_splash:
  image: "assets/branding/icon.png"
  image_dark: "assets/branding/icon_dark.png"
  color: "#EFE0C9"
  color_dark: "#33261C"
  branding: "assets/branding/wordmark.png"
  branding_dark: "assets/branding/wordmark_dark.png"
  branding_mode: bottom

  android_12:
    image: "assets/branding/icon.png"
    image_dark: "assets/branding/icon_dark.png"
    icon_background_color: "#EFE0C9"
    color: "#EFE0C9"
    icon_background_color_dark: "#33261C"
    color_dark: "#33261C"
    branding: "assets/branding/wordmark.png"
    branding_dark: "assets/branding/wordmark_dark.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

  # Bundled locally so the app stays fully offline (no network font fetch at
  # runtime, unlike the `google_fonts` package it replaces). Each family
  # ships as the single upstream variable-font file (OFL-licensed, sourced
  # from https://github.com/google/fonts) — none of Newsreader, Work Sans or
  # Caveat publish static per-weight files upstream. A single asset entry per
  # family/style is intentional: Flutter reads the font's `wght` axis and
  # interpolates any requested FontWeight from it, so declaring the same file
  # again per weight is unnecessary (and unsupported the same way static
  # font sets are). See assets/fonts/OFL-*.txt for each family's license.
  fonts:
    - family: Newsreader
      fonts:
        - asset: assets/fonts/Newsreader-VariableFont_opsz-wght.ttf
        - asset: assets/fonts/Newsreader-Italic-VariableFont_opsz-wght.ttf
          style: italic
    - family: Work Sans
      fonts:
        - asset: assets/fonts/WorkSans-VariableFont_wght.ttf
    - family: Caveat
      fonts:
        - asset: assets/fonts/Caveat-VariableFont_wght.ttf
